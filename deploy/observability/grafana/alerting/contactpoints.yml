apiVersion: 1

contactPoints:
  - name: "Email Admins"
    receivers:
      - uid: "email-admins"
        type: email
        settings:
          addresses: "alert@optimizesolux.com"
        disableResolveMessage: false
